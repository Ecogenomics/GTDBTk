.. code-block:: bash

    # NOTE: replace 2.5.0 with the version you wish to install

    # using conda
    conda create -n gtdbtk-2.5.0 -c conda-forge -c bioconda gtdbtk=2.5.0

    # using mamba (alternative)
    mamba create -n gtdbtk-2.5.0 -c conda-forge -c bioconda gtdbtk=2.5.0
