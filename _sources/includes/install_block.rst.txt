.. code-block:: bash

    # NOTE: replace 2.5.1 with the version you wish to install

    # using conda
    conda create -n gtdbtk-2.5.1 -c conda-forge -c bioconda gtdbtk=2.5.1

    # using mamba (alternative)
    mamba create -n gtdbtk-2.5.1 -c conda-forge -c bioconda gtdbtk=2.5.1
